<% provide(:title, 'All users') %>
<h2> All users </h2>
  <div class="pagination">
    <%= will_paginate %>
  </div>
  <ul class="users">
      <%= render @users %>
  </ul>
  <div class="pagination">
    <%= will_paginate %>
  </div>
